created: 20131219100608529
modified: 20150508121616331
tags: [[TiddlyWiki on Node.js]]
title: Installing TiddlyWiki on Node.js
type: text/vnd.tiddlywiki

# Installing [[Node.js]] from http://nodejs.org
# Open a command line terminal and type:
#> `npm install -g tiddlywiki`
#> If it fails with an error you may need to re-run the command as an administrator:
#> `sudo npm install -g tiddlywiki` (Mac/Linux)
#* The `-g` flag causes TiddlyWiki to be installed globally. Without it, TiddlyWiki will only be available in the directory where you installed it.<br /><br />
# Check TiddlyWiki is installed by typing:
#> `tiddlywiki --version`
# In response, you should see TiddlyWiki report its current version (eg "<<version>>"; you may also see other debugging information reported)
# Create an empty folder manually [optionnal]:
#> `cd /path/to/your_working_directory` 
#> `mkdir YourFolder`
#* You can also use your file explorer to create the folder.<br /><br />
# Initialise an empty TiddlyWikiFolders with a copy of the specified edition.
#> `tiddlywiki YourFolder --init VersionToUse`
#> To find help on this command: `tiddlywiki --help init`
#> Replace the `VersionToUse` by one of the edition provided here:<br />`tiddlywiki --editions`
#* Example:<br />`tiddlywiki ~/Test/MyWikiFolder --init tw5.com`<br /><br />
# To start your new TiddlyWiki:
#> `tiddlywiki ~/Test/MyWikiFolder --server`
#> Visit http://127.0.0.1:8080/ in your browser
#> Try editing, creating and saving tiddlers
# To create a folder for a new wiki that includes server-related components [Optionnal]: 
#>[[How to enable a wiki folder for client-server usage]]

If you are using Debian or Debian-based Linux and you are receiving a `node: command not found` error though node.js package is installed, you may need to create a symbolic link between `nodejs` and `node`. Consult your distro's manual and `whereis` to correctly create a link. See github [[issue 1434|http://github.com/Jermolene/TiddlyWiki5/issues/1434]]

Example Debian v8.0: `sudo ln -s /usr/bin/nodejs /usr/bin/node`
