created: 20260305203308065
modified: 20260305203321452
tags: 
title: New Tiddler
type: text/vnd.tiddlywiki

Anyone already taking this? I will start trying implementing this in several days.

To use wiki as agents memory, I need have a stable way to record to do and track progress.

* [x] Add new wiki parser rule, checkbox, And in render engine, it renders to checkbox widget.
** [x] It's an inline rule. Can be used  together with nested list rule, Or contain multiple checkboxes in a single line. Like in GitHub markdown. `- [ ]`
* [ ] Wiki parser accepts a new option, tiddlerTitle. This pass to the root node of AST. Then pass to every widget, exists as a field on rootWidget. So every widget knows the tiddler title it belongs to.
** [x] Need to unit test transclude widget properly modify the target widget's text, not the current tiddler's text.
* [ ] Since we already have a stable start end position in AST node, No new method or utilities needed. Simply in checkbox onclick callback, replace the text range and use `addTiddler` To update the tiddler.

# [ ] Need to update the style, Make list hide the dot when it has a checkbox as first element In list item.
# [ ] While numbered list don't have to do this.

As a plugin or leave to community:

* [ ] New filter operator `unchecked` `checked` To get all unchecked checkboxes from the input titles. `tiddlerWith:checkbox[ ]` Get all titles with whose texts has a checkbox.
* [ ] tiddlerWithSyntax indexer like backlink indexer, To scan all tiddler to get everyone with Certain syntax like checkbox. This will make operators above O(1) when run in a map or loop.